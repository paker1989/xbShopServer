const _AUTH_ADMIN_ID_NOT_PRESENT = 'idAdmin is not presented';

const _AUTH_ADMIN_EMAIL_NOT_PRESENT = 'admin email is not presented';

const _AUTH_ADMIN_EMAIL_DUPLICATED = 'admin email already exist';

const _AUTH_ROLE_LINKED_ADMINS = 'user.addRole.link.admin';

module.exports = {
    _AUTH_ADMIN_ID_NOT_PRESENT,
    _AUTH_ADMIN_EMAIL_NOT_PRESENT,
    _AUTH_ADMIN_EMAIL_DUPLICATED,
    _AUTH_ROLE_LINKED_ADMINS,
};
