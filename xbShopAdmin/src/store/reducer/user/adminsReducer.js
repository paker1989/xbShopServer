/**
 * add product form
 */
// import * as userActionType from '../../actionType/userActionType';

const initialState = {};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
