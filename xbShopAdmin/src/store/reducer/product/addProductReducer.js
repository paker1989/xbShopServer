/**
 * add product form
 */
const initialState = {
    currentStep: 0,
    productName: '测试产品',
    short_dscp: '',
    category: [],
};

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
